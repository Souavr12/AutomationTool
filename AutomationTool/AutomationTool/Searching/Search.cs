using System;

public static class Search<T>
{
    public static T Linear(this List<T> array, T value)
    {
        foreach (T indComponent in array)
        {
            if (indComponent == value)
            {
                return indComponent;
            }
        }
    }

    public static T Binary(this List<T> array, T value)
    {
        int low = array.
        foreach (T indComponent in array)
        {
            if (indComponent == value)
            {
                return indComponent;
            }
        }
    }
}
